<%= simple_form_for(@street) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :no_via %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
