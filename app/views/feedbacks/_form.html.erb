<%= simple_form_for([@user,@service,@feedback]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :tx_comentario %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
