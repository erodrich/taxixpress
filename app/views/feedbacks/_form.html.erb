<%= simple_form_for([@user,@service,@feedback]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :tx_comentario, label: "Coméntenos: ", input_html: { class: 'form-control' }%>
  </div>

  <div class="form-actions">
    <%= f.submit "Enviar", :class =>'btn btn-primary' %>
  </div>
<% end %>
